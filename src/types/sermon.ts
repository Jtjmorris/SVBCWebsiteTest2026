export interface Sermon {
    id: string
    title: string
    speaker: string
    date: string
    series: string
    duration?: string
    thumbnail: string
    scripture: string
    videoUrl?: string
}
